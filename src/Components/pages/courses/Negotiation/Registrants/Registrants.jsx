import React from'react';
import './Registrants.css';
const Registrants = ()=>{

    return(
        <div className="registrants__innerWrap">
            
        </div>
    );
}

export default React.memo(Registrants);